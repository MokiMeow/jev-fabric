---
"@mokimeow/jev-fabric-packs": minor
---

Add a `finance-research-router` pack that batches prohibited-intent,
untrusted-influence, fixed-tool, allowlisted-symbol, and semantic-window
judgments into one advisory request. It can propose only read-only market
research tools, requires host revalidation, and routes unsafe, unsupported,
malformed, stale, or influenced cases to review without exposing an execution
channel.
