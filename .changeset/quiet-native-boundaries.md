---
"@mokimeow/jev-fabric-provider-typesafe": patch
---

Reject TypeSafe API-invalid null state and empty Noul meaning before dispatch,
pin a userland HTTP transport that passes post-header cancellation probes for
native SDK requests, and retain an offline wire-contract test for the fixed
Vercel Gateway method, path, bearer authentication, and Jev model identity. The
public Gateway factory no longer accepts a client override that could bypass
that route.
