---
"@mokimeow/jev-fabric-packs": minor
---

Add the critical, routing-only `fintech-exception` pack. It batches six
independent Noul questions over one redacted, hash-bound case note and can only
propose `observe`, `investigate`, or `escalate`; malformed answers, urgent harm,
untrusted influence, and provider failures escalate, while invalid execution
boundaries deny before provider use. The state boundary rejects future or
expired evidence, calendar-invalid timestamps, hostile object shapes, unknown
fields, and candidate or hash substitution without invoking accessors.
