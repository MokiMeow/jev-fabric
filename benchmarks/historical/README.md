# Historical laboratory imports

Imports are strictly local exploratory evidence. They do not establish live or held-out performance. Previous choice ECE based on TypeSafe `confidence` is retained only as an invalid historical confidence/accuracy statistic; it is not probability calibration. New ECE is computed only from recorded distributions and outcomes.
