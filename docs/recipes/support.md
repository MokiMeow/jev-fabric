# SaaS support routing
Define queues in trusted configuration, project only the ticket facts needed for a route, and use `route` to select a queue or abstain. Customer entitlement, refunds, account changes, and outbound messages remain separate checked workflows. See [support-triage](../../examples/support-triage/README.md).
