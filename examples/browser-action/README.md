# Browser action advice
Returns advice to ask the user before navigation. It contains no browser-control import and never clicks, types, downloads, signs in, or submits. The negative path abstains. Run `pnpm exec tsx examples/browser-action/index.ts`.
