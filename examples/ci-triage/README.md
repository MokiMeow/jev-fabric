# CI triage
Screens bounded build/test candidates using scripted offline data. It does not change a CI status; a host must enforce checks and escalation. The empty-candidate path fails closed. Run `pnpm exec tsx examples/ci-triage/index.ts`.
