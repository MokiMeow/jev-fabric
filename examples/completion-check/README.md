# Completion check
Assesses a completion claim only when declared checks are observed. The negative state claims an unobserved integration check and receives a deterministic deny without a provider call. Run `pnpm exec tsx examples/completion-check/index.ts`.
