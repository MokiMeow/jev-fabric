# Evaluate a pack
Uses `progress` with scripted data to classify a bounded workflow state. It is an offline example of runtime evaluation and receipt validation. The empty candidate path abstains. Run `pnpm exec tsx examples/evaluate-pack/index.ts`.
