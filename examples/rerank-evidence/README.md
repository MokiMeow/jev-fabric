# Rerank evidence
Chooses a supplied primary source over a supplied secondary source. It does not retrieve documents or create citations; the host owns both. The empty evidence path abstains. Run `pnpm exec tsx examples/rerank-evidence/index.ts`.
