# Route skills
Selects `coding` from two declared skill lanes with the `route` pack. The companion empty-candidate case abstains without calling a provider. Run `pnpm exec tsx examples/route-skills/index.ts`; expected output includes `"name":"route-skills"` and redacted receipts.
