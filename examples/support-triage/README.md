# Support triage
Routes a ticket to one declared queue. Billing actions, entitlement decisions, and messages remain outside this example. The empty queue case abstains. Run `pnpm exec tsx examples/support-triage/index.ts`.
