# Tool-environment advice

Returns advisory text to ask the user before a declared persistent operation. It
contains no browser, Blender, engine, CAD, command, script, or network import;
it never observes or executes a native action. The negative path abstains. Run
`pnpm exec tsx examples/tool-environment-advice/index.ts`.
