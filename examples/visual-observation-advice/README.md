# Visual-observation advice

This offline example binds a bounded, untrusted visual annotation to a hashed
browser projection, then advises requesting structured data. It loads no image,
uses no browser/DCC/CAD client, and never clicks, types, executes, or authorizes
an action. The negative path abstains. Run
`pnpm exec tsx examples/visual-observation-advice/index.ts`.
