# jev-fabric-adapter/v1; generated; advisory/non-executing; do not grant permissions automatically
# Copy this section into Codex config.toml. The companion Codex plugin ships the shared skill.

[mcp_servers.jev-fabric]
command = "jev-fabric"
args = ["serve","--transport","stdio"]
